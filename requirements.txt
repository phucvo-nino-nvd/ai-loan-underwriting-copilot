# Backend serving dependencies (CPU-only; no GPU needed for inference).
# Use Python 3.11 or 3.12; some packages may not have wheels for newer versions yet.

# ML inference
lightgbm>=4.3.0
xgboost>=2.0.0
catboost>=1.2.0
shap>=0.44.0

# Data handling
numpy>=1.26.0
pandas>=2.2.0
polars>=1.0.0
pyarrow>=15.0.0

# LLM layer
openai>=1.40.0
python-dotenv>=1.0.0

# API layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
fastapi-clerk-auth>=0.0.7
